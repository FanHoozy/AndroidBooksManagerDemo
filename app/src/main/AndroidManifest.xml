<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.booksmanagerdemo11">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BooksManagerDemo11">
        <activity android:name=".ui.BookManager.LendManagerActivity" android:label= "借阅管理"></activity>
        <activity android:name=".ui.BookManager.BooksManageActivity" android:label= "图书管理"/>
        <activity android:name=".ui.BookManager.ShowLendedBookActivity" android:label= "查询已借"/>
        <activity android:name=".ui.login.LoginActivity" android:label= "登录界面">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
    </application>

</manifest>